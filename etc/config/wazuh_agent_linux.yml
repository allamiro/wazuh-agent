agent:
  thread_count: 4
  server_url: https://localhost:27000
  retry_interval: 30s
  verification_mode: none
  queue_size: 10000
events:
  batch_interval: 10s
  batch_size: 1MB
inventory:
  enabled: true
  interval: 1h
  scan_on_start: true
  hardware: true
  system: true
  networks: true
  packages: true
  ports: true
  ports_all: false
  processes: false
logcollector:
  enabled: true
  localfiles:
    - /var/log/auth.log
  reload_interval: 1m
  read_interval: 500ms
  journald:
    - field: PRIORITY
      value: 0|1|2|3|4|5|6|7
      exact_match: true
      ignore_if_missing: true
  # Agent-side syslog listeners (UDP/TCP). Disabled by default: no listener is
  # started unless explicitly configured here. bind_address defaults to 127.0.0.1.
  # syslog:
  #   - protocol: udp
  #     bind_address: 127.0.0.1
  #     port: 5514
  #   - protocol: tcp
  #     bind_address: 0.0.0.0
  #     port: 1514
  #     allowed_ips:            # optional source allow-list (IPs/CIDRs); empty allows any
  #       - 10.10.0.0/16
  #       - 192.168.1.5
